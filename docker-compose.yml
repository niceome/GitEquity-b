services:
  postgres:
    image: postgres:16
    container_name: gitequity-postgres
    environment:
      POSTGRES_DB: gitequity
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
